export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Spectra",
  description: "Collect, analyze and aggregate data",
  keywords: "aptos, data, collect, analyze, aggregate",
};
